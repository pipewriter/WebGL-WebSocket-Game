window.drawPicture = (function drawPicture(){
    await initializeWebGL;    
    let vertexBuffer = gl.createBuffer();
    gl.bindBuffer(gl.ARRAY_BUFFER,vertexBuffer);
    var vertexArray = [];
    var indexArray = [];
    
    
})();